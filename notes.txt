-- GOF has issues with defaults checking
-- Check input box size with subjectExclusions table
-- Example for specifying exclusions:

ObsExc <- "Keep"
ObsExc[ DV < 0.05 ] <- "BQL"
ObsExc[ TAFD > 100 ] <- "EOS"

SubjExc <- "Keep"
SubjExc[ RACE=="88" | RACE==88 ] <- "Missing race"


-- Commit ID cbff82970 works with 434source.csv